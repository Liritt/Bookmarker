services:
  bookmarker:
    build: .
    volumes:
      - ./settings.py:/app/settings.py:ro
    depends_on:
      - postgres
  postgres:
    image: postgres:15
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: bookmarker
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root